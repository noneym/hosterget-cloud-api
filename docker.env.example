# Docker Environment Configuration Example
# Copy this file to docker.env and fill in your actual values

# Database Configuration (Required)
DATABASE_URL=postgresql://user:password@your-postgres-host:5432/hosterget
PGHOST=your-postgres-host
PGPORT=5432
PGUSER=your-postgres-user
PGPASSWORD=your-postgres-password
PGDATABASE=hosterget

# Session Secret (Required - Generate with: openssl rand -base64 32)
SESSION_SECRET=your-random-32-plus-character-secret-key

# Stripe Configuration (Required)
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
VITE_STRIPE_PUBLIC_KEY=pk_live_your_stripe_public_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Test Keys (Optional - for testing)
TESTING_STRIPE_SECRET_KEY=sk_test_your_test_key
TESTING_VITE_STRIPE_PUBLIC_KEY=pk_test_your_test_key

# Application Configuration
NODE_ENV=production
PORT=5000

# OIDC Configuration (Optional)
ISSUER_URL=https://replit.com/oidc

# Replit Specific (Usually NOT needed for Docker deployments)
# Only required if:
# 1. You're running Docker container ON Replit platform itself
# 2. You're using Replit Auth and need redirect URLs to work
# These values are auto-set on Replit (find with: echo $REPL_ID)
# For external Docker deployments, you can safely OMIT these
# REPL_ID=your-repl-id
# REPLIT_DOMAINS=your-domain.replit.app
